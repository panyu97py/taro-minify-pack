{
  "name": "taro-minify-pack",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "clear-dist": "rm -rf dist && pnpm -r exec rm -rf dist",
    "build": "pnpm clear-dist && pnpm --filter @taro-minify-pack/* build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
  }
}
